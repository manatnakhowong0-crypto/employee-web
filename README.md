# 📊 ระบบข้อมูลพนักงาน (Employee Management System)

ระบบดูข้อมูลการมาทำงานของพนักงาน — ดึงข้อมูลจาก Google Sheets
หน้าหลักเปิดดูได้เลย (ไม่ต้องล็อกอิน) ส่วน "รายงาน / จัดการ / การแก้ไข" ต้องเข้าสู่ระบบก่อน

---

## 📁 โครงสร้างไฟล์

```
employee-system/
├── index.html                  ← หน้าหลัก (สาธารณะ) = ตารางสรุปมาทำงาน ★ ไม่ต้องล็อกอิน
├── pages/
│   ├── dashboard.html          ← คนมาวันนี้ (สาธารณะ)
│   ├── search.html             ← ค้นหาตามวันที่ (🔒 ต้องล็อกอิน — รายงาน)
│   ├── employee.html           ← ประวัติทั้งหมด (🔒 ต้องล็อกอิน — รายงาน)
│   ├── leave.html              ← แจ้งลา + ปฏิทิน (🔒 ต้องล็อกอิน — จัดการ)
│   └── advance.html            ← เบิกเงินล่วงหน้า (🔒 ต้องล็อกอิน — จัดการ)
├── css/
│   ├── main.css                ← Global + Sidebar + เมนูมือถือ + กล่องล็อกอิน + responsive
│   ├── attendance.css          ← สไตล์ตาราง Attendance grid
│   └── pages.css               ← สไตล์ Dashboard, Search, Leave
├── js/
│   ├── config.js               ← API URL + รหัสล็อกอิน + ค่าตั้งต้น
│   ├── auth.js                 ← ล็อกอิน/ออกจากระบบ + กล่องล็อกอิน (modal)
│   ├── shell.js                ← Sidebar + เมนูมือถือ (ใช้ร่วมทุกหน้า)
│   ├── api.js                  ← ดึง/ส่งข้อมูลกับ Google Sheets
│   ├── attendance.js           ← Logic ตาราง + แก้ไขเวลาเข้างาน (เขียนลงชีตถาวร)
│   └── pages.js                ← Logic Search, Leave, Calendar, Employee
└── README.md
```

> หมายเหตุ: ไฟล์ `Code.gs` (สำหรับ Google Apps Script) อยู่แยกนอก zip — ต้องนำไปวางใน Apps Script เอง

---

## 🔐 การเข้าสู่ระบบ

หน้าหลัก (ดูวันทำงาน) **เข้าดูได้เลย ไม่ต้องล็อกอิน**
ต้องล็อกอินเฉพาะเมื่อจะ: เปิดหน้า **รายงาน**, หน้า **จัดการ**, หรือ **แก้ไข/เพิ่ม/ลบ** เวลาเข้างาน

| Username    | Password      |
|-------------|---------------|
| `sanaethai` | `sanaethai15` |

- ใช้ `localStorage` — เข้าสู่ระบบค้างไว้จนกว่าจะกด "ออกจากระบบ"
- เป็นการกันแก้ไขเบื้องต้นฝั่งหน้าเว็บ — โปรเจกต์จริงควรตรวจรหัสที่ฝั่งเซิร์ฟเวอร์ด้วย

---

## 📊 ตาราง Attendance Grid (หน้าหลัก)

- เลือก **พนักงาน / ปี / เดือน** ได้อิสระ
- สีตามเวลาเข้างาน: 🟢 ปกติ (ก่อน 09:30) · 🟡 สาย (09:31–11:00) · 🟠 ครึ่งวัน (หลัง 11:00) · 🔴 ขาด · ⬜ เสาร์–อาทิตย์
- การ์ดสรุปยอดรวมทั้งเดือน
- **เมื่อล็อกอินแล้ว** คลิกที่ช่องเพื่อ **แก้ไข / เพิ่มย้อนหลัง / ลบ** เวลาเข้างานได้ — บันทึกลงชีต "ชีต1" ถาวร

---

## ✏️ การแก้ไขเวลาแบบถาวร (สำคัญ)

การแก้ไขบนหน้าเว็บจะเขียนลง **"ชีต1"** โดยตรง (ไม่ใช่เก็บแค่ในเครื่อง):

- **แก้เวลา** วันที่มีข้อมูลอยู่แล้ว → อัปเดตคอลัมน์ "เวลาเริ่มต้น" ของแถวนั้น
- **เพิ่มย้อนหลัง** (มาทำงานแต่ไม่ได้เช็คชื่อ) → เพิ่มแถวใหม่ในชีต1
- **ลบ** → ลบแถวของวันนั้นออกจากชีต1

> ⚠️ ต้องนำ `Code.gs` เวอร์ชันใหม่ไปวางใน Apps Script แล้ว **Deploy ใหม่** ฟีเจอร์นี้จึงทำงาน (ดูหัวข้อถัดไป)

---

## 🔧 ตั้งค่า Google Apps Script

1. เปิด Google Sheet → เมนู **ส่วนขยาย (Extensions) → Apps Script**
2. ลบโค้ดเดิม แล้ววางเนื้อหาจากไฟล์ `Code.gs` ใหม่ทั้งหมด → บันทึก
3. กด **Deploy → New deployment** (หรือ Manage deployments → แก้ไขอันเดิม) แบบ Web app
   - Execute as: **Me**
   - Who has access: **Anyone**
4. คัดลอก URL ที่ได้ มาวางใน `js/config.js` ที่ `API_URL`
   (ถ้าใช้ URL เดิมและแก้แบบ "Manage deployments" ไม่ต้องเปลี่ยน URL)

```js
const CONFIG = {
  API_URL: "https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec",
  ...
};
```

---

## 📱 รองรับทั้งคอมและมือถือ

- จอกว้าง: Sidebar แสดงถาวรด้านซ้าย
- จอมือถือ: Sidebar ซ่อน แตะปุ่ม **☰** มุมซ้ายบนเพื่อเปิดเมนู
